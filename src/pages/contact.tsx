import React from "react";

import { Container } from "../lib/container.ts";
import { Heading } from "../lib/text.ts";

const Contact: React.FC = () => (
  <Container>
    <Heading>Contact Page</Heading>
  </Container>
);

export default Contact;
